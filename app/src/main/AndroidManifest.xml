<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.weebly.stevelosk.sewingpatternapp">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddPatternActivity" />
        <activity android:name=".BasicSearchActivity"
            android:configChanges="orientation|keyboardHidden"/>
        <activity android:name=".TestDatabaseActivity" />
        <activity android:name=".BrowseActivity" />
        <activity android:name=".ExaminePatternActivity" />
        <activity android:name=".AdvancedSearchActivity" />
        <activity android:name=".ImageCloseUp"></activity>
    </application>

</manifest>